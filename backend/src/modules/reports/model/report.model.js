// Report — no persisted collection. The reports module is purely aggregational: it derives
// analytics on-the-fly from Vendor / RFQ / Quotation / PurchaseOrder / Invoice via the repository.
// Exported as an empty object so the module folder stays structurally consistent with the others.
module.exports = {};
